import '../styles/Auth.css'

export function Auth(props) {
    const renderElement = () => {
        if(props.type == "register"){
            return 
        }
    }

    return (
        <div>{renderElement}</div>
    );
}